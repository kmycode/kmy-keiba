<Window x:Class="KmyKeiba.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="KMY競馬" Height="700" Width="1000">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <DockPanel Grid.Row="0">
      <Menu DockPanel.Dock="Top">
        <MenuItem Header="ファイル(_F)">
          <MenuItem Header="JV-Linkからデータをロード(_L)" Command="{Binding OpenJVLinkLoadDialogCommand}"/>
          <Separator/>
          <MenuItem Header="終了(_X)"/>
        </MenuItem>
      </Menu>
    </DockPanel>
    <TabControl Grid.Row="1" ItemsSource="{Binding Tabs}"
                ContentTemplateSelector="{StaticResource MainTabContentTemplateSelector}">
      <TabControl.ItemTemplate>
        <DataTemplate>
          <TextBlock Text="レース一覧" Width="200" Height="32" FontSize="20"
                     VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </DataTemplate>
      </TabControl.ItemTemplate>
    </TabControl>
  </Grid>
</Window>
